# Filestack Demo

to run the code locally, you'll need to create a `config.js` file in the
project's root directory and define a config object in it which holds the
filestack api key:

```
export const config = {
  filestackKey: "your key here"
};
```
